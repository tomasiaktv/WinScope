<UserControl x:Class="WinScope.App.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0"
                  ItemsSource="{Binding Processes}"
                  SelectedItem="{Binding SelectedProcess}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,12,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="120"/>
                <DataGridTextColumn Header="Memory (MB)" Binding="{Binding MemoryMB}" Width="140"/>
                <DataGridTextColumn Header="CPU (%)" Binding="{Binding CpuPercent}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Column="1" Padding="12" BorderBrush="Gray" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Details" FontSize="16" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding SelectedProcess.Name}" />
                <TextBlock Text="{Binding SelectedProcess.Pid}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
